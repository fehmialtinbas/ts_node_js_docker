version: "3.7"

services:
  mfa-ts-app:
    build:
      context: .
      dockerfile: Dockerfile
      target: base
    container_name: mfa-ts-app
    expose:
      - "8088"
    ports:
      - "8088:8088"
    command: npm run start
